# Petey TODO

## Features
- [ ] Page-chunked extraction: split a multi-page PDF into chunks (e.g., 1 page each) and extract each chunk independently with the same schema. Useful for documents where each page has the same structure (officer lists, case summaries, etc.). CLI flag: `--pages-per-chunk N`
